
import { FC, memo } from "react";
import {{title}} from "./{{title}}";
interface {{title}}ContainerProps {

}

const {{title}}Container: React.FC<{{title}}ContainerProps> = props => {

    const {

    } = props;

    return (
        <>
              <{{title}}/>
        </>
    );

}

export default memo({{title}}Container);